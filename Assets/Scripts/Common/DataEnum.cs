
public enum PopupType
{
    CreateNicName,
    ModifyNicName,
}

public enum GameType
{
    Game1,
    Game2,
    Game3,
}

public enum SceneType
{
    LobyScene,
    RoomScene,
    TestScene,
}

enum MoveState
{
    Idle,
    Move,
}